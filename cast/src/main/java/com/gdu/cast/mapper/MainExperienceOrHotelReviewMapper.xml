<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gdu.cast.mapper.MainExperienceOrHotelReviewMapper">
	<!-- experience comment list-->
	<select id="experiencePaymentReview" parameterType="Map" resultType="com.gdu.cast.vo.ExperiencePaymentReview">
		SELECT 
		experience_payment_review_id experiencePaymentReviewId,
		experience_id experienceId,
		experience_payment_id experiencePaymentId,
		experience_payment_review_content experiencePaymentReviewContent,
		create_date createDate,
		update_date updateDate 
		FROM experience_payment_review
		WHERE experience_id = #{experienceId}
		ORDER BY create_date DESC
		LIMIT #{beginRow}, #{ROW_PER_PAGE}
	</select>

		<!--  experience select comment list 페이징 -->
	<select id="selectExperienceReviewTotalCount" parameterType="String" resultType="int">
	SELECT COUNT(*) 
	FROM experience_payment_review
	</select>
</mapper>